[pytest]
# Register custom marks to suppress PytestUnknownMarkWarning
markers =
    offline: Tests that run in offline mode (no Gemini API key required).
    integration: End-to-end pipeline tests spanning multiple modules.

# Always show short test summary at end
addopts = -ra
